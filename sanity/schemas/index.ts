import about from "./about-schema";
import contact from "./contact-schema";
import projectImage from "./projectImage-schema";
import projects from "./projects-schema";
import welcome from "./welcome-schema";

export const schemas = [welcome, about, projects, projectImage, contact];
